[build]
  command = "hugo --gc --minify"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.123.0"
  NODE_VERSION = "18"
  HUGO_ENV = "production"
  HUGO_ENABLEGITINFO = "true"
  GIT_SUBMODULE_STRATEGY = "recursive"
